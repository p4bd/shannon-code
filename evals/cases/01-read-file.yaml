{
  "name": "read file",
  "prompt": "Read README.md and summarize it.",
  "workspace": {
    "files": {
      "README.md": "# Fixture\nhello eval\n"
    }
  },
  "mock": {
    "steps": [
      {
        "toolCalls": [
          { "name": "read_file", "input": { "path": "README.md" } }
        ]
      },
      { "content": "Read README.md." }
    ]
  },
  "assertions": [
    { "type": "tool_called", "name": "read_file" },
    { "type": "session_contains", "text": "hello eval" }
  ]
}

