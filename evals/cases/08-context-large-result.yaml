{
  "name": "context large result",
  "prompt": "Read huge.txt.",
  "workspace": {
    "files": {
      "huge.txt": { "repeat": "x", "count": 35000 }
    }
  },
  "mock": {
    "steps": [
      {
        "toolCalls": [
          { "name": "read_file", "input": { "path": "huge.txt" } }
        ]
      },
      { "content": "Large result was stored." }
    ]
  },
  "assertions": [
    { "type": "tool_called", "name": "read_file" },
    { "type": "session_contains", "text": "Tool result was too large" },
    { "type": "session_contains", "text": ".agent/artifacts/" }
  ]
}

